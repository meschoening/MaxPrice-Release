{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability granting renderer access to the get_sidecar_url command. The sidecar process is spawned by Rust via app.shell().sidecar(), so the renderer does not need shell:allow-spawn. core:window:default is read-only, so show/set-focus are granted explicitly: the main window is created hidden and the renderer puts it on screen once the boot splash has painted (ADR-0066).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "dialog:allow-open",
    "opener:allow-open-path",
    "updater:default",
    "process:default"
  ]
}
