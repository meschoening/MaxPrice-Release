// Compile-time constants injected by Vite's `define` (see vite.config.ts).

// The hub app version, baked in from apps/hub-desktop/package.json at build
// time. Surfaced by the header pill.
declare const __APP_VERSION__: string;
